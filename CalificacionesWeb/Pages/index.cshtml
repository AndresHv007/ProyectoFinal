@page

@{
 Layout = "Shared/_InicioSesionLayout.cshtml";
}

@model CalificacionesWeb.pages.IndexModel;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<body>
  <div class="container">
    <div class="row">
      <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
        <div class="card card-signin my-5">
          <div class="card-body">
            <h5 class="card-title text-center">Iniciar Sesion</h5>
            <form class="form-signin" method="POST">
              <div class="form-label-Grupo">
                <input asp-for="Cuenta" class="form-control" placeholder="Cuenta" required autofocus>
                <label for="inputEmail">Cuenta</label>
              </div>
              <div class="form-label-Grupo">
                <input asp-for="Contraseña" class="form-control" placeholder="Contraseña" required>
                <label for="inputContraseña">Contraseña</label>
              </div>
              <button class="btn btn-lg btn-primary btn-block text-uppercase" type="submit">Iniciar Sesion</button>
              <hr class="my-4">
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  @if (Model.Error)
  {
    <div class="alert alert-danger">
      <strong>Error!</strong> Datos incorrectos
    </div>
  }

</body> 